# ──────────────────────────────────────────────
# bug-reporter-react Test App — Environment
# ──────────────────────────────────────────────
# Copy this file to .env and fill in the values.
# Vite exposes variables prefixed with VITE_ to the client.

# ── Linear Integration ──
VITE_LINEAR_API_KEY=
VITE_LINEAR_TEAM_ID=
VITE_LINEAR_PROJECT_ID=
VITE_LINEAR_SUBMIT_PROXY_ENDPOINT=/api/bug-report

# ── Jira Integration ──
VITE_JIRA_BASE_URL=
VITE_JIRA_EMAIL=
VITE_JIRA_API_TOKEN=
VITE_JIRA_PROJECT_KEY=
VITE_JIRA_ISSUE_TYPE=Bug
VITE_JIRA_SUBMIT_PROXY_ENDPOINT=/api/bug-report

# ── General ──
# Which provider to use by default: "linear" or "jira"
VITE_DEFAULT_PROVIDER=linear
